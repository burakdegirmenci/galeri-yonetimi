# Database Configuration
# Coolify will automatically provide this when you add a PostgreSQL service
# No manual configuration needed for Coolify deployment
DATABASE_URL="postgresql://postgres:password@localhost:5432/galeri_db"

# JWT Secret Key (OPTIONAL)
# If not provided, a random secret will be auto-generated
# For persistent sessions across restarts, set this value
# Generate using: openssl rand -base64 32
JWT_SECRET=""

# Node Environment
NODE_ENV="production"
