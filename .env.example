# Database Configuration
# Default: SQLite (zero-config, works immediately)
# For production with PostgreSQL, change to: postgresql://user:pass@host:5432/db
DATABASE_URL="file:./prod.db"

# JWT Secret Key (OPTIONAL)
# If not provided, a random secret will be auto-generated
# For persistent sessions across restarts, set this value
# Generate using: openssl rand -base64 32
JWT_SECRET=""

# Node Environment
NODE_ENV="production"
